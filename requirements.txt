# =============================================
# 권장 Python 버전: 3.10.x (3.10.11 또는 3.10.14), 지금 쓰는 버전 : 3.11.9
# =============================================
# 설치 명령어:
#   pip install -r requirements.txt

# YOLO / 딥러닝
ultralytics>=8.3.0
torch>=2.0.0
torchvision>=0.15.0

# 영상 처리
opencv-python>=4.8.0

# 이미지 처리 (한글 텍스트 렌더링)
Pillow>=10.0.0

# 수치 계산
numpy>=1.24.0,<2.0.0   # numpy 2.x는 torch 호환 이슈 있음

# PostgreSQL 연결  ← 파이프라인 추가로 신규
psycopg2-binary>=2.9.9